{
    "rules": {
      "admins": {
      },
      "users": { 
        ".read": "root.child('admins').child(auth.uid).val() != null", 
        "$uid": { 
          ".read": "auth != null && auth.uid == $uid"
        } 
      }
    }
}